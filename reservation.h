#ifndef RESERVATION_H
#define RESERVATION_H

class reservation
{private :
   int num_res;
int cin_p;
int num_v;
    public:
        reservation(num_res,);
        virtual ~reservation();


};

#endif // RESERVATION_H
